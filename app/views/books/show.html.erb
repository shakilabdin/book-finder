<h1><%= @book.title %></h1>
<h2><%= link_to @book.author.name, author_path(@book.author) %></h2>
<h2><%= link_to @book.genre.name, genre_path(@book.genre) %></h2>
<p><%= @book.summary %></p>

<%= button_to "All Books", books_path, method: :get %>
<%= button_to "Write Review", new_review_path(@book)%>